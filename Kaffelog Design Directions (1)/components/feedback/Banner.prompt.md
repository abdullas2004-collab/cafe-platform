Page/section-level banner and a floating toast, both left-border-coded by tone.

```jsx
<Banner tone="error" title="Pest control contract expires in 3 days" action={<Button size="sm">Renew</Button>} />
<Toast tone="success" message="August log exported" />
```
